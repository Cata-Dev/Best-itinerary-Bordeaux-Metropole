import { theme, toggleDarkMode } from './theme/theme'
import { client, APIRefresh, socket } from './feathers/feathers'

export {
    toggleDarkMode,
    theme,
    client,
    APIRefresh,
    socket,
}